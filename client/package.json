{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "clean": "cd ../program && cargo clean",
    "build": "cd ../program && cargo build-sbf",
    "deploy": "solana program deploy ../program/target/deploy/solana_intro.so",
    "start": "ts-node index.ts",
    "test": "pnpm build && pnpm start",
    "all": "pnpm clean && pnpm build && pnpm deploy && pnpm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.27.0",
  "dependencies": {
    "@solana-program/system": "^0.10.0",
    "@solana/web3.js": "^2.0.0"
  },
  "devDependencies": {
    "@types/expect": "^24.3.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
